{
  "name": "geov4",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "engines": { "node": ">=24.12.0" },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel -r dev",
    "test": "pnpm --filter @geov4/domain build && pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
